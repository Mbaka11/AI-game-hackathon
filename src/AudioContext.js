import React from 'react';
// Create the context with a default value
export const AudioContext = React.createContext({
  audioId: '', // Default audio source
  setAudioId: () => {}, // Placeholder function
});